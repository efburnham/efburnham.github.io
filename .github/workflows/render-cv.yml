name: Compile CV PDF
on:
  push:
    branches:
      - main
      - master
    paths:
      - "_data/cv.yml"
      - "_bibliography/papers.bib"
      - "assets/cv/template.typ"
      - "bin/compile_cv.py"
  workflow_call:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  compile-cv:
    name: Compile CV
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Verify typst is available
        run: python3 -c "import typst; print('typst', typst.__version__)"
      - name: Compile CV
        run: python3 bin/compile_cv.py
      - name: Upload PDF artifact
        uses: actions/upload-artifact@v4
        with:
          name: BurnhamEmilieCV
          path: assets/pdf/BurnhamEmilieCV.pdf
      - name: Push changes
        run: |
          git config --global user.name "${{ github.actor }}"
          git config --global user.email "${{ github.actor }}@users.noreply.github.com"
          git add assets/pdf/BurnhamEmilieCV.pdf assets/cv/cv_data.yml _data/cv_pdf.yml
          if git diff --cached --quiet; then
            echo "No CV changes to commit."
            exit 0
          fi
          git commit -m "chore: compile CV PDF"
          git push
